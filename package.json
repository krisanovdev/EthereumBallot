{
  "name": "ethereumelections",
  "version": "1.0.0",
  "description": "POC of honest elections.",
  "dependencies": {
    "ethereumjs-tx": "^1.3.7",
    "web3": "^1.0.0-beta.34",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "truffle": "^5.0.19"
  },
  "scripts": {
    "clear-build": "rm -rf build",
    "build": "npm run-script clear-build && truffle compile",
    "elections:deploy": "node deployment/deploy_ballot_contract.js"
  }
}
